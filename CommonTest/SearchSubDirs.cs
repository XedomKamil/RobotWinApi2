using System;
using System.Collections.Generic;
using System.IO;
using Common;
using Microsoft.VisualStudio.TestTools.UnitTesting;
//using System.Windows.Forms;
namespace CommonTest
{
    [TestClass]
    public class SearchSubDirs
    {
        [TestMethod]
        public void TestMethod1()
        {
            /*
            // Stwóz
            FileOperations fileOperations = new FileOperations();
            // Przelicz
            string path = (@"E:\PBL\Prof Białecki Programy\wetransfer-9d2a73\Nowy folder\view frame\WindowsFormsApp1\WindowsFormsApp1\bin\Debug\");
            fileOperations.GetFoldersInDir(path);
            Console.WriteLine();
            Console.WriteLine();
            Console.WriteLine("New test");

            path = (@"E:\PBL\Prof Białecki Programy\wetransfer-9d2a73\Nowy folder\view frame\WindowsFormsApp1\");
            string[] dirs = fileOperations.GetFoldersInDir(path);
            foreach (var dir in dirs)
            {
               string name = new DirectoryInfo(dir).Name;
               // Label label = new Label();
               // label.Text = new DirectoryInfo(dir).Name;
                Console.WriteLine("Name :");
                Console.WriteLine(name);
            }

            path = (@"E:\");

            List<string> list = new List<string>();
            list = fileOperations.SearchFiles(path);
            foreach (var item in list)
            {
                Console.WriteLine(item);
            }
            */

        }
    }
}
